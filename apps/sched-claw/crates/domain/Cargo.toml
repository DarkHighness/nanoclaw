[package]
name = "sched-claw-domain"
edition = "2024"
license = "MIT"
version = "0.1.0"

[dependencies]
anyhow = "1.0.100"
serde = { version = "1.0.228", features = ["derive"] }
toml = { workspace = true }

[dev-dependencies]
tempfile = "3.23.0"
