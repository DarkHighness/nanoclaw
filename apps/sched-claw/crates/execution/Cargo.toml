[package]
name = "sched-claw-execution"
edition = "2024"
license = "MIT"
version = "0.1.0"

[dependencies]
sched-claw-daemon-protocol = { path = "../daemon-protocol" }
sched-claw-domain = { path = "../domain" }
anyhow = "1.0.100"
clap = { version = "4.5.38", features = ["derive"] }
tokio = { version = "1.48.0", features = ["process", "rt", "time"] }

[dev-dependencies]
tempfile = "3.23.0"
